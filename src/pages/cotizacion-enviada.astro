---
import MainLayout from '../layouts/MainLayout.astro';
import { siteConfig } from '../data/site-config';
---

<MainLayout title="Cotización Enviada | Gel Chile" description="Tu solicitud de cotización ha sido enviada exitosamente.">
  <section class="success-page">
    <div class="container">
      <div class="success-card" data-aos="fade-up">
        <div class="success-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14" />
            <polyline points="22 4 12 14.01 9 11.01" />
          </svg>
        </div>

        <h1>¡Cotización Enviada!</h1>

        <p class="success-message">
          Hemos recibido tu solicitud de cotización correctamente.
          Nuestro equipo la revisará y te contactaremos a la brevedad.
        </p>

        <div class="next-steps">
          <h2>¿Qué sigue?</h2>
          <div class="steps-list">
            <div class="step-item">
              <span class="step-number">1</span>
              <div class="step-content">
                <strong>Revisión</strong>
                <p>Nuestro equipo revisará tu solicitud y los productos seleccionados.</p>
              </div>
            </div>
            <div class="step-item">
              <span class="step-number">2</span>
              <div class="step-content">
                <strong>Cotización Formal</strong>
                <p>Te enviaremos una cotización detallada a tu correo electrónico.</p>
              </div>
            </div>
            <div class="step-item">
              <span class="step-number">3</span>
              <div class="step-content">
                <strong>Contacto</strong>
                <p>Un ejecutivo se comunicará contigo para resolver cualquier duda.</p>
              </div>
            </div>
          </div>
        </div>

        <div class="contact-info">
          <p>¿Tienes dudas? Contáctanos:</p>
          <div class="contact-links">
            <a href={`tel:${siteConfig.contact.phones[0].replace(/\s/g, '')}`} class="contact-link">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="20" height="20">
                <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
              </svg>
              {siteConfig.contact.phones[0]}
            </a>
          </div>
        </div>

        <div class="actions">
          <a href="/productos" class="btn btn-primary">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="20" height="20">
              <circle cx="9" cy="21" r="1"/>
              <circle cx="20" cy="21" r="1"/>
              <path d="m1 1 4 4 2 14h13"/>
            </svg>
            Seguir Explorando Productos
          </a>
          <a href="/" class="btn btn-outline">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="20" height="20">
              <path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
              <polyline points="9 22 9 12 15 12 15 22"/>
            </svg>
            Volver al Inicio
          </a>
        </div>
      </div>
    </div>
  </section>
</MainLayout>

<style>
  .success-page {
    padding: calc(72px + var(--spacing-12)) 0 var(--spacing-16);
    min-height: 80vh;
    display: flex;
    align-items: center;
  }

  .success-card {
    max-width: 600px;
    margin: 0 auto;
    text-align: center;
    background: var(--color-white);
    border-radius: var(--radius-xl);
    padding: var(--spacing-12);
    box-shadow: var(--shadow-lg);
  }

  .success-icon {
    width: 80px;
    height: 80px;
    margin: 0 auto var(--spacing-6);
    background: linear-gradient(135deg, var(--color-success) 0%, #059669 100%);
    border-radius: var(--radius-full);
    display: flex;
    align-items: center;
    justify-content: center;
    animation: scaleIn 0.5s ease-out;
  }

  .success-icon svg {
    width: 40px;
    height: 40px;
    color: white;
  }

  h1 {
    font-size: var(--font-size-3xl);
    color: var(--color-gray-900);
    margin-bottom: var(--spacing-4);
  }

  .success-message {
    font-size: var(--font-size-lg);
    color: var(--color-gray-600);
    margin-bottom: var(--spacing-8);
    line-height: 1.6;
  }

  .next-steps {
    background: var(--color-gray-50);
    border-radius: var(--radius-lg);
    padding: var(--spacing-6);
    margin-bottom: var(--spacing-8);
    text-align: left;
  }

  .next-steps h2 {
    font-size: var(--font-size-lg);
    color: var(--color-gray-900);
    margin-bottom: var(--spacing-4);
    text-align: center;
  }

  .steps-list {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-4);
  }

  .step-item {
    display: flex;
    align-items: flex-start;
    gap: var(--spacing-3);
  }

  .step-number {
    width: 28px;
    height: 28px;
    background: var(--color-primary);
    color: white;
    border-radius: var(--radius-full);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-bold);
    flex-shrink: 0;
  }

  .step-content strong {
    display: block;
    color: var(--color-gray-900);
    margin-bottom: var(--spacing-1);
  }

  .step-content p {
    font-size: var(--font-size-sm);
    color: var(--color-gray-600);
    margin: 0;
  }

  .contact-info {
    margin-bottom: var(--spacing-8);
  }

  .contact-info > p {
    font-size: var(--font-size-sm);
    color: var(--color-gray-500);
    margin-bottom: var(--spacing-2);
  }

  .contact-links {
    display: flex;
    justify-content: center;
    gap: var(--spacing-4);
  }

  .contact-link {
    display: inline-flex;
    align-items: center;
    gap: var(--spacing-2);
    color: var(--color-primary);
    font-weight: var(--font-weight-medium);
    transition: color var(--transition-fast);
  }

  .contact-link:hover {
    color: var(--color-primary-dark);
  }

  .actions {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-3);
  }

  .actions .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--spacing-2);
  }

  @keyframes scaleIn {
    0% {
      transform: scale(0);
      opacity: 0;
    }
    50% {
      transform: scale(1.1);
    }
    100% {
      transform: scale(1);
      opacity: 1;
    }
  }

  /* Dark mode */
  :global([data-theme="dark"]) .success-card {
    background: #1e293b;
  }

  :global([data-theme="dark"]) h1 {
    color: #f1f5f9;
  }

  :global([data-theme="dark"]) .success-message {
    color: #94a3b8;
  }

  :global([data-theme="dark"]) .next-steps {
    background: #0f172a;
  }

  :global([data-theme="dark"]) .next-steps h2,
  :global([data-theme="dark"]) .step-content strong {
    color: #f1f5f9;
  }

  :global([data-theme="dark"]) .step-content p {
    color: #94a3b8;
  }

  :global([data-theme="dark"]) .contact-info > p {
    color: #64748b;
  }

  /* Responsive */
  @media (max-width: 640px) {
    .success-card {
      padding: var(--spacing-8) var(--spacing-4);
    }

    h1 {
      font-size: var(--font-size-2xl);
    }

    .success-message {
      font-size: var(--font-size-base);
    }
  }
</style>
